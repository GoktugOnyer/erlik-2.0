# Minimum versions — kept loose so pip installs wheels compatible with the
# user's Python (pinned-exact versions fail to build on newer Python releases).
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0
aiosqlite>=0.20
httpx>=0.27
pydantic>=2.9,<3
jinja2>=3.1
python-multipart>=0.0.9
pyyaml>=6.0
cryptography>=45          # Fernet for credential encryption at rest (orchestrator/secrets.py)

# The thesis analysis scripts need scipy; it is not imported by the orchestrator,
# so it lives in requirements-analysis.txt to keep this install path light.
