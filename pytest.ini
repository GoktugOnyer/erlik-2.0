[pytest]
# The scientific-instrument test suite. These tests pin the behaviour of the
# scoring / finding-detection functions the thesis metrics depend on, so a
# refactor (or a recall-roadmap change) cannot silently move the numbers.
testpaths = tests
python_files = test_*.py
addopts = -ra
