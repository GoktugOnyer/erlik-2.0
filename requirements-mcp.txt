# Optional dependency for the MCP servers under mcp_servers/ ONLY.
# The core orchestrator does NOT import `mcp` — keep this out of requirements.txt.
#   pip install -r requirements-mcp.txt
mcp>=1.0.0
# httpx is already pinned in requirements.txt (the NVD core uses it).
